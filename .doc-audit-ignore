# Named in prose; they live in ../bore-generator, not in this repository.

bore_split.py
check.py
regress.py

# Boxes.py is the checkout bore_split shells out to for the finger joints and
# the SVG writer. Named because the rebuild block runs its venv python.

Boxes.py

# The mouthpiece and the bell live in ../trumpet-parts: neither is touched by
# the way a bore turns. Named here so a builder knows which sheets to cut with
# this bore. They are the same 10mm pair ../trumpet-switchback uses.

mouthpiece-bore10-trumpet-parts-cut-files.svg
bell-round10-153mm-17rings-x3-rim86-cut-files.svg
