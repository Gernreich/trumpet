# Names this repository uses in prose but does not ship, and never will.
#
# The parts are generated by boxes.py, the web tool at
# https://boxes.hackerspace-bamberg.de/ by Hackerspace Bamberg, using its RegularBox
# generator. Neither is a file here. RegularBox.svg is the name that site serves every
# download as, which is why the writeup tells you to rename each of the three runs as
# you save it -- three downloads in a row would otherwise collide.

boxes.py
RegularBox.svg

# Named in prose; it lives in ../bore-ribbon, not here. torus-turn.html is
# generated by it, because a rectangular section swept along a planar curve is
# what that repository draws and this torus is one.

ribbon_view.py
